name: Release Go Binary

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            suffix: linux-amd64
          - goos: linux
            goarch: arm64
            suffix: linux-arm64
          - goos: darwin
            goarch: amd64
            suffix: darwin-amd64
          - goos: darwin
            goarch: arm64
            suffix: darwin-arm64
          - goos: windows
            goarch: amd64
            suffix: windows-amd64
            ext: .exe
            asset_name: godot-mcp-server.exe

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          cache-dependency-path: mcp-server-go/go.sum

      - name: Build
        working-directory: mcp-server-go
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: go build -o ../godot-mcp-server-${{ matrix.suffix }}${{ matrix.ext }} ./cmd/godot-mcp-server

      - name: Rename for release
        if: matrix.asset_name
        run: mv godot-mcp-server-${{ matrix.suffix }}${{ matrix.ext }} ${{ matrix.asset_name }}

      - uses: actions/upload-artifact@v4
        with:
          name: godot-mcp-server-${{ matrix.suffix }}
          path: ${{ matrix.asset_name || format('godot-mcp-server-{0}{1}', matrix.suffix, matrix.ext) }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*
          generate_release_notes: true
